iiname: 'SSH4Me'
author: 'jisan09'
description: 'Build SSH tunnel using ngrok over workflow'

runs:
  using: 'composite'
  steps:
    - name: 'Set up Docker'
      shell: bash
      run: |
        curl -fsSL https://get.docker.com -o get-docker.sh
        sudo sh get-docker.sh

    - name: 'Build Docker image'
      shell: bash
      run: |
         
+50
It is not something about order. The Dockerfile must be specified with -f

     run: |
        docker build -t ssh4me --mode=max --build-arg AUTH_TOKEN=$AUTH_TOKEN --build-arg PASSWORD=$PASSWORD .
    - name: 'Run Docker container'
      shell: bash
      run: docker run-it --dns 1.1.1.1 --dns 1.0.0.1 docker.io/library/ssh4me 
  

branding:
  icon: 'terminal'
  color: 'gray-dark'
